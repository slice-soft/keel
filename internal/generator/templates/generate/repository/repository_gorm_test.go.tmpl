package {{.PackageName}}

import (
	"testing"

	"github.com/slice-soft/ss-keel-gorm/database"
)

func Test{{.PascalName}}RepositoryStruct(t *testing.T) {
	// Compile-time check: *{{.PascalName}}Repository satisfies core.Repository[{{.PascalName}}Entity, string]
	// via the embedded GormRepository. No database connection needed for this test.
	var _ interface {
		DB() interface{}
	} = (*database.GormRepository[{{.PascalName}}Entity, string])(nil)
}
