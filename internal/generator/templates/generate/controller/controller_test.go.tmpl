package {{.PackageName}}

import (
	"testing"

	"github.com/slice-soft/ss-keel-core/logger"
)

func Test{{.PascalName}}Controller(t *testing.T) {
	ctrl := New{{.PascalName}}Controller(nil, logger.NewLogger(false))
	if ctrl == nil {
		t.Fatal("expected controller instance")
	}

	if len(ctrl.Routes()) == 0 {
		t.Fatal("expected at least one route")
	}
}
