package checkers

import (
	"context"

	"github.com/slice-soft/ss-keel-core/core"
)

type {{.PascalName}}Checker struct{}

var _ core.HealthChecker = (*{{.PascalName}}Checker)(nil)

func New{{.PascalName}}Checker() *{{.PascalName}}Checker {
	return &{{.PascalName}}Checker{}
}

func (c *{{.PascalName}}Checker) Name() string {
	return "{{.KebabName}}"
}

func (c *{{.PascalName}}Checker) Check(ctx context.Context) error {
	_ = ctx
	return nil
}
