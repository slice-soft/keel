package {{.PackageName}}

import "github.com/slice-soft/ss-keel-mongo/mongo"

// {{.PascalName}}Entity is the domain entity for this module.
// mongo.EntityBase provides ID, CreatedAt and UpdatedAt with BSON tags pre-configured.
type {{.PascalName}}Entity struct {
	mongo.EntityBase
	// TODO: replace this placeholder with your real domain fields.
	Name string `json:"name"`
}
