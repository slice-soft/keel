package {{.PackageName}}

type Create{{.PascalName}}RequestDTO struct {
	Name string `json:"name" validate:"required"`
}

// Update{{.PascalName}}RequestDTO is used for HTTP PUT — all fields are required and the record is fully replaced.
type Update{{.PascalName}}RequestDTO struct {
	Name string `json:"name" validate:"required"`
}

// Patch{{.PascalName}}RequestDTO is used for HTTP PATCH — all fields are optional; only non-nil fields are written.
type Patch{{.PascalName}}RequestDTO struct {
	Name *string `json:"name"`
}

type {{.PascalName}}ResponseDTO struct {
	ID        string `json:"id"`
	Name      string `json:"name"`
	CreatedAt int64  `json:"created_at"`
	UpdatedAt int64  `json:"updated_at"`
}
